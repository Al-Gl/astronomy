---
import MainLayout from '../../layouts/MainLayout.astro';
---

<MainLayout
  title="Thank You - The Astronomy Space"
  description="Thank you for contacting The Astronomy Space"
>
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="success-icon">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
          <polyline points="22 4 12 14.01 9 11.01"></polyline>
        </svg>
      </div>
      <h1 class="hero-title">Message Sent!</h1>
      <p class="hero-subtitle">
        Thank you for reaching out. We've received your message and will get back to you within 24-48 hours.
      </p>
      <div class="actions">
        <a href="/" class="btn-primary">Return to Home</a>
        <a href="/atlas/" class="btn-secondary">Explore the Cosmos</a>
      </div>
    </div>
  </section>

  <!-- Additional Info -->
  <section class="info-section">
    <div class="container">
      <div class="info-grid">
        <div class="info-card">
          <h2>What's Next?</h2>
          <p>We'll review your message and respond via email as soon as possible. Make sure to check your spam folder if you don't see our reply.</p>
        </div>

        <div class="info-card">
          <h2>While You Wait</h2>
          <ul>
            <li><a href="/atlas/">Explore our Cosmic Atlas</a></li>
            <li><a href="/chronicles/">Read the latest Chronicles</a></li>
            <li><a href="/observatory/">Check out astronomy equipment</a></li>
          </ul>
        </div>
      </div>
    </div>
  </section>
</MainLayout>

<style>
  /* Hero Section */
  .hero {
    padding: 6rem 0 4rem 0;
    text-align: center;
    min-height: 70vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .success-icon {
    width: 80px;
    height: 80px;
    margin: 0 auto 2rem auto;
    color: rgba(6, 182, 212, 0.9);
    animation: checkmark 0.5s ease-in-out;
  }

  @keyframes checkmark {
    0% {
      transform: scale(0) rotate(-45deg);
      opacity: 0;
    }
    50% {
      transform: scale(1.1) rotate(5deg);
    }
    100% {
      transform: scale(1) rotate(0deg);
      opacity: 1;
    }
  }

  .success-icon svg {
    width: 100%;
    height: 100%;
  }

  .hero-title {
    font-size: clamp(2.5rem, 8vw, 4rem);
    font-weight: 400;
    margin: 0 0 1.5rem 0;
    letter-spacing: 0.02em;
    color: white;
    line-height: 1.1;
  }

  .hero-subtitle {
    font-size: 1.125rem;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.6);
    max-width: 40rem;
    margin: 0 auto 3rem auto;
    line-height: 1.8;
  }

  /* Action Buttons */
  .actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-primary,
  .btn-secondary {
    display: inline-block;
    padding: 0.875rem 2rem;
    border-radius: 4px;
    font-size: 0.9375rem;
    font-weight: 500;
    letter-spacing: 0.02em;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .btn-primary {
    background: rgba(6, 182, 212, 0.15);
    border: 1px solid rgba(6, 182, 212, 0.3);
    color: rgba(6, 182, 212, 0.9);
  }

  .btn-primary:hover {
    background: rgba(6, 182, 212, 0.25);
    border-color: rgba(6, 182, 212, 0.5);
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.15);
    color: rgba(255, 255, 255, 0.8);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.25);
    transform: translateY(-2px);
  }

  /* Info Section */
  .info-section {
    padding: 2rem 0 6rem 0;
  }

  .info-grid {
    display: grid;
    gap: 2rem;
    max-width: 800px;
    margin: 0 auto;
    grid-template-columns: 1fr;
  }

  @media (min-width: 768px) {
    .info-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .info-card {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 8px;
    padding: 2rem;
  }

  .info-card h2 {
    font-size: 1.25rem;
    font-weight: 500;
    color: white;
    margin: 0 0 1rem 0;
    letter-spacing: 0.01em;
  }

  .info-card p {
    font-size: 0.9375rem;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.7;
    margin: 0;
  }

  .info-card ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .info-card li {
    margin-bottom: 0.75rem;
  }

  .info-card li:last-child {
    margin-bottom: 0;
  }

  .info-card a {
    color: rgba(6, 182, 212, 0.9);
    text-decoration: none;
    font-size: 0.9375rem;
    font-weight: 300;
    transition: color 0.2s ease;
  }

  .info-card a:hover {
    color: rgba(6, 182, 212, 1);
  }

  /* Container */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }
</style>
